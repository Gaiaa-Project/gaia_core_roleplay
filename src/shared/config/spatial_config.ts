export const SpatialConfig = {
  mapMinX: -3700,
  mapMaxX: 4500,
  mapMinY: -4400,
  mapMaxY: 8000,
  gridCellSize: 100,
  pointCheckInterval: 500,
  zoneCheckInterval: 300,
  defaultNearbyRadius: 150,
} as const;
